{
  "name": "seed-ui",
  "version": "0.0.1",
  "description": "ui framework",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx parcel index.html --no-cache --no-minify",
    "build": "parcel build index.html --no-cache --no-minify"
  },
  "keywords": [
    "ui",
    "framework"
  ],
  "author": "sethlee",
  "license": "MIT",
  "dependencies": {
    "vue": "^2.6.10"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.3"
  }
}
